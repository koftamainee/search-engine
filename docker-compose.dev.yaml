services:
  backend:
    image: ""
    build: ./backend
    ports:
      - "8080"
    environment:
      - CONFIG_PATH=config/local.json

  crawler:
    image: ""
    build:
      context: crawler
      dockerfile: Dockerfile
      target: development
    volumes:
      - ./crawler:/app

  frontend:
    image: ""
    build: ./frontend
    ports:
      - "80"

  redis-backend:
    ports:
      - "6379:6379"

  reverse_proxy:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx/dev.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - frontend
      - backend