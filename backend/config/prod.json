{
  "env": "prod",
  "http_server": {
    "address": "0.0.0.0:8080",
    "timeout": 10000000000,
    "idle_timeout": 60000000000
  },
  "postgres": {
    "url": "postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}"
  },
  "meilisearch": {
    "url": "http://${MEILI_HOST}:${MEILI_PORT}",
    "api_key": "${MEILI_MASTER_KEY}",
    "index": "web_pages"
  },
  "session_storage": {
    "address": "${SESSION_STORAGE_HOST}:${SESSION_STORAGE_PORT}",
    "password": "${SESSION_STORAGE_PASSWORD}",
    "DB": 0
  }
}